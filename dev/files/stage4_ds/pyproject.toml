[project]
name = "midas-stage4-databricks"
version = "0.1.0"
description = "Stage 4 Databricks agent for EPM MIDAS ordenes de calidad"
requires-python = ">=3.10"
dependencies = ["requests>=2.31.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
